name: "pwnkit-action-deprecated"
description: "Deprecated wrapper for the legacy action/ path"

runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        echo "::error::The legacy 'action/' entrypoint has been retired. Use 'peaktwilight/pwnkit@<ref>' from the repository root instead."
        exit 1
