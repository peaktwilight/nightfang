# pwnkit

> Open-source agentic framework for autonomous security research.

pwnkit uses 4 AI agents (Discover, Attack, Verify, Report) to find and prove vulnerabilities. It scans LLM endpoints, MCP servers, npm packages, source code, and web applications.

## Quick Start

```bash
npx pwnkit-cli scan https://your-app.com/api/chat
npx pwnkit-cli audit express
npx pwnkit-cli review ./my-repo
```

## Key Features

- **Autonomous multi-agent pipeline**: 4 specialized AI agents run in sequence — Discover, Attack, Verify, Report
- **Verification eliminates false positives**: The Verify agent re-exploits every finding independently. If it can't reproduce it, the finding is dropped
- **5 attack surfaces**: LLM endpoints, MCP servers, npm packages, source code, web apps
- **5 commands**: scan, audit, review, findings, history
- **Multi-runtime**: Works with Claude Code, Codex, Gemini CLI, OpenCode, or API-only mode
- **SARIF output**: Findings appear directly in GitHub's Security tab
- **GitHub Action**: Drop into CI/CD with `peaktwilight/pwnkit/action@v1`
- **Zero config**: `npx pwnkit-cli scan` — no YAML, no setup

## Built From Real Research

pwnkit was built from the methodology that found 7 CVEs in npm packages with 40M+ weekly downloads, including node-forge (32M/week), mysql2 (5M/week), Uptime Kuma (86K stars), LiquidJS, jsPDF, and picomatch.

## Links

- Website: https://pwnkit.com
- GitHub: https://github.com/peaktwilight/pwnkit
- npm: https://www.npmjs.com/package/pwnkit-cli
- Author: https://doruk.ch
- CVE writeups: https://doruk.ch/blog
